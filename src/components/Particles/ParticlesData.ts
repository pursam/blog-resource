type Particle = {
  name: string;
  image: string;
  tags: string;
  particle: string;
}

const particles: Particle[] = [
  {
  name: 'Lorem Ipsum Dollor Amett',
  image: 'string',
  tags: 'travel photo',
  particle: 'string',
  }
]

export default particles